# Backend (FastAPI)
# Copy to .env (or export in your shell). Do not commit real secrets.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Frontend (Vite)
# Create web/.env.local with the following (values from Supabase project settings):
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
