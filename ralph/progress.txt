# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-02-26
---

## 2026-02-26 21:50 - US-UI-001
- Scaffolded Vite 7 + React 19 + TypeScript app in `web/` with React Router 7, Tailwind CSS 4, and manual shadcn/ui Button component
- Configured Vite dev proxy (`/api/*` → `http://localhost:8000/*`), vitest 4 test suite (6 tests pass), and production build (tsc -b && vite build)
- Added web dev/build/test commands to AGENTS.md and web/README.md; fixed .gitignore to allow `web/src/lib/`
- Files changed: `web/` (all scaffold files), `AGENTS.md`, `.gitignore`
- Learnings: bun resolves latest versions (React 19, Router 7, Vite 7, Tailwind 4, Vitest 4) — use `vitest/config` defineConfig (not vite's) for proper test type resolution; Tailwind v4 uses `@import "tailwindcss"` + `@tailwindcss/postcss` plugin; `bun test` runs bun's runner, not vitest — use `bun run test` to invoke the vitest script
---

## 2026-02-27 09:49 - US-UI-002
- Added Supabase JS client (`web/src/lib/supabase.ts`), `AuthContext` with magic-link signIn/signOut, and `RequireAuth` guard component
- Protected `/games` and `/g/:gameId` routes; unauthenticated users redirect to `/login`; Login form submits magic link and shows confirmation/error; Games page has Log out button
- Tests: 11 vitest tests pass (auth redirect, magic link form, logout); TypeScript lint and production build both clean; added `vite/client` to tsconfig types for `import.meta.env`
- Files changed: `web/src/lib/supabase.ts` (new), `web/src/context/AuthContext.tsx` (new), `web/src/components/RequireAuth.tsx` (new), `web/src/App.tsx`, `web/src/routes/Login.tsx`, `web/src/routes/Games.tsx`, `web/src/__tests__/routes.test.tsx`, `web/tsconfig.app.json`, `web/package.json`, `web/bun.lock`
- Learnings: `vi.mock` is hoisted so use `vi.hoisted()` to declare shared mock functions; add `"vite/client"` to tsconfig `types` array alongside `"vitest/globals"` so `import.meta.env` type-checks correctly
---

## 2026-02-27 - US-UI-003
- Added `app/auth.py` with `get_caller_id` FastAPI dependency (validates Supabase JWT via PyJWT, reads `SUPABASE_JWT_SECRET`); all endpoints now use `CallerIdDep` for identity
- Removed `player_id` from all request bodies; `POST /games` auto-joins caller as player1; `GET /games` lists caller's games; `GET /games/{id}` blocks non-participants on ACTIVE/COMPLETE games (403)
- Added `LastMoveInfo` (mists_roll, mists_effect) to `GameState`; reducer now tracks mists roll per move; SD rounds alternate starting player via `starting_player_index`; starting player on ACTIVE chosen deterministically from seed
- Added migration `003_idempotency_key_and_player_columns.sql`; updated SupabaseGameStore to sync `player1_id`/`player2_id` columns and implement `list_games_for_player`
- Tests: 432 pass (pytest), ruff + pyright clean; added `tests/test_auth_api.py` covering GET /games, spectator block, last_move, deterministic starting player
- Files: `app/auth.py` (new), `app/models/game.py`, `app/rules/reducer.py`, `app/services/game_service.py`, `app/store/__init__.py`, `app/store/memory.py`, `app/store/supabase_store.py`, `app/routers/games.py`, `supabase/migrations/003_*` (new), plus 6 test files
- Learnings: PyJWT is available as transitive dep via supabase package; test auth with `app.dependency_overrides[get_caller_id] = lambda request: request.headers.get("X-User-Id")`; `begin_sudden_death_round` uses `(starting_player_index + round_number - 1) % 2` formula for alternation
---

## 2026-02-28 16:15 - US-UI-010
- Added Supabase Realtime subscription in `GameRoom.tsx`: subscribes to `game_events` INSERT events filtered by `game_id` via `supabase.channel("game:${gameId}").on(...).subscribe(...)` following `docs/realtime.md` contract
- On INSERT, refetches `GET /games/:gameId` and replaces local snapshot state; unsubscribes (`supabase.removeChannel`) on unmount; starts 30s fallback polling interval when channel status is CLOSED/CHANNEL_ERROR, clears it on SUBSCRIBED
- Tests: 4 new vitest tests (52 total pass), 432 pytest pass; ruff + pyright + vite build clean
- Files changed: `web/src/routes/GameRoom.tsx`, `web/src/__tests__/routes.test.tsx`
- Learnings: `vi.hoisted()` blocks are independent — place realtime mocks in their own block and extend the `vi.mock()` factory to include `channel`/`removeChannel`; capture handlers by mutating a shared `realtimeCbs` object from the mock implementation; reset `realtimeCbs` fields in `beforeEach` since `vi.clearAllMocks()` does not clear mock implementations; use `vi.useFakeTimers()` locally in a test (with try/finally + `vi.useRealTimers()`) to test interval behavior; `mockGetGame.mock.calls.length` counts calls synchronously so it works even before the returned promise resolves
---

## 2026-02-28 16:00 - US-UI-009
- Added `powerOptions` optional param to `placeCard` in `api.ts` (options object for `useArchetype`, `skulkerBoostSide`, `presenceBoostDirection`)
- Added "Use Power" checkbox toggle to GameRoom active view (visible when caller has unused archetype, on their turn); Skulker/Presence show n/e/s/w side selector on toggle; cells blocked until side selected for those archetypes
- Tests: 6 new vitest tests (48 total pass), 432 pytest pass; ruff + pyright + vite build clean
- Files changed: `web/src/lib/api.ts`, `web/src/routes/GameRoom.tsx`, `web/src/__tests__/routes.test.tsx`
- Learnings: When extending mock call signatures with an optional arg, using `undefined` explicitly vs omitting the arg matters for `toHaveBeenCalledWith` checks — use an `if/else` branch to call with or without the extra arg; `getByText(/archetype/i)` can collide with checkbox label text — keep label text generic (no archetype name) to avoid multi-match errors in existing tests
---

## 2026-02-27 15:45 - US-UI-008
- Added `selectArchetype(gameId, archetype)` to `web/src/lib/api.ts`; throws typed error with `.status` on failure
- Added archetype selection prompt in GameRoom ACTIVE view: shows 5 archetype buttons when caller has no archetype; hides after selection; displays selected archetype + Available/Used status for both players
- Tests: 5 new vitest tests (42 total pass), 432 pytest pass; ruff + pyright + vite build clean
- Files changed: `web/src/lib/api.ts`, `web/src/routes/GameRoom.tsx`, `web/src/__tests__/routes.test.tsx`
- Learnings: add new mocked API functions to both `vi.hoisted()` and `vi.mock()` blocks AND `beforeEach` defaults to avoid test bleed; Archetype type exported from api.ts avoids duplication in GameRoom
---

## 2026-02-27 15:30 - US-UI-007
- Added `placeCard(gameId, cardKey, cellIndex, stateVersion, idempotencyKey)` to `web/src/lib/api.ts`; throws error with `.status` property on non-OK responses
- Updated `BoardGrid` to accept `canPlace`/`onCellClick` props; empty cells become `<button aria-label="cell N">` when it's the caller's turn and a card is selected
- Added `handlePlaceCard`, `moveError`, and `movePending` states to `GameRoom.tsx`; card hand buttons disabled when not caller's turn; 409/422 errors shown + game refetched
- Tests: 6 new vitest tests (37 total pass), 432 pytest pass; ruff + pyright + vite build clean
- Files changed: `web/src/lib/api.ts`, `web/src/routes/GameRoom.tsx`, `web/src/__tests__/routes.test.tsx`
- Learnings: attach `.status` to Error via `Object.assign(err, { status })` instead of a custom class to avoid mock complexity; `crypto.randomUUID()` is available in jsdom test env; render empty cells as `<button>` elements (not divs) to leverage `getByRole("button", { name: /cell N/ })` in tests
---

## 2026-02-27 15:19 - US-UI-006
- Added `BoardCell`, `GameResult` types and `board`/`current_player_index`/`result` fields to `GameState` in `api.ts`; added `leaveGame(gameId, stateVersion)` function (handles 204 no-content)
- Implemented `BoardGrid` component (3×3 grid, blue=mine/red=opponent ownership); active game view: turn indicator, score, hand (selectable buttons), opponent card count, archetype display, Leave button; complete game view: result banner (win/lose/draw), final board, Leave button; Leave navigates to /games
- Added 11 vitest tests covering all US-UI-006 acceptance criteria; all 31 vitest pass, 432 pytest pass; ruff + pyright + vite build clean
- Files changed: `web/src/lib/api.ts`, `web/src/routes/GameRoom.tsx`, `web/src/__tests__/routes.test.tsx`
- Learnings: make a `makeGame(overrides)` factory in tests to avoid duplicating full GameState shapes; `aria-label` on the grid div allows `getByLabelText` in tests; catch-all in `handleLeave` ensures navigation even if API fails
---

## 2026-02-27 10:46 - US-UI-005
- Added `getGame(gameId)` and `joinGame(gameId)` to `web/src/lib/api.ts`
- Implemented `GameRoom.tsx` lobby UI: fetches game on mount; WAITING renders invite link + Copy Link button; shows Join button when not a participant and game not full; shows blocked message when full and not a participant; ACTIVE/COMPLETE show placeholder text
- Added 5 new vitest tests covering all lobby states; all 20 vitest pass, 432 pytest pass; ruff + pyright + vite build clean
- Files changed: `web/src/routes/GameRoom.tsx`, `web/src/lib/api.ts`, `web/src/__tests__/routes.test.tsx`
- Learnings: `useAuth().user.id` is the caller identity for participant checks; default `mockGetGame` in `beforeEach` prevents existing navigation tests from erroring when they land on `/g/:gameId`
---

## 2026-02-27 10:11 - US-UI-004
- Created `web/src/lib/api.ts` with `listGames()` and `createGame()` — authenticated fetch wrappers that read the Supabase session token and call `/api/games`
- Updated `web/src/routes/Games.tsx` to fetch and render the game list, show status badges, navigate to `/g/:gameId` on row click, and navigate after Create Game
- Added 4 new vitest tests (games list render, row click navigation, Create Game flow, empty state); fixed pre-existing ruff format/lint issues in 6 Python files
- Tests: 15 vitest pass, 432 pytest pass; ruff + pyright clean; vite build clean
- Files changed: `web/src/lib/api.ts` (new), `web/src/routes/Games.tsx`, `web/src/__tests__/routes.test.tsx`, `tests/test_idempotency.py` (lint fix), 5 other pre-existing format-only changes
- Learnings: mock `@/lib/api` with `vi.hoisted()` + `vi.mock()` for clean API isolation; ruff format does not auto-break mock `.return_value` chains — split them into intermediate variables
---

