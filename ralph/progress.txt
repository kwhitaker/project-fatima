# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-03-01
---

## 2026-03-01 15:30 - US-UX-004
- Added `ArchetypeNotSelectedError(InvalidMoveError)` to `app/rules/errors.py`; raised in `submit_move` before `apply_intent` when `player.archetype is None` → router maps to 422 with actionable detail
- Replaced inline archetype selection prompt in `GameRoom.tsx` with a fixed `role="dialog" aria-modal="true"` overlay; added `!!myPlayer?.archetype` guard to `canPlace` and hand card `disabled` prop to block all move interactions until archetype chosen
- Tests: `test_ux004.py` (6 backend tests: error class hierarchy, 422 on missing archetype, 422 detail is actionable, 200 after selection, second player also blocked); `ux004.test.tsx` (5 UI tests: modal shown/hidden, all 5 archetype options present, hand disabled without archetype, hand enabled after); 463 backend + 83 frontend tests pass; updated `moveGame`/`effectsGame` fixtures in `routes.test.tsx` and setup helpers in `test_error_responses.py`, `test_idempotency.py`, `test_api.py`, `test_auth_api.py` to select archetypes before move submission
- Files: `app/rules/errors.py`, `app/services/game_service.py`, `web/src/routes/GameRoom.tsx`, `tests/test_ux004.py`, `web/src/__tests__/ux004.test.tsx`, `web/src/__tests__/routes.test.tsx`, `tests/test_error_responses.py`, `tests/test_idempotency.py`, `tests/test_api.py`, `tests/test_auth_api.py`, `ralph/prd.json`, `ralph/progress.txt`
- Pattern: pre-existing tests that submit moves without archetype selection need their setup helpers updated; `ArchetypeNotSelectedError` must extend `InvalidMoveError` (not `ValueError`) to be caught by the existing `except InvalidMoveError` → 422 path in the router
---

## 2026-03-01 14:30 - US-UX-003
- Added `completion_reason: str | None` and `forfeit_by_index: int | None` to `GameResult` model (backward-compatible, defaults to None)
- Updated `compute_round_result` and `begin_sudden_death_round` (SD cap draw) to set `completion_reason="normal"`
- Updated `leave_game` in `game_service.py` to set `completion_reason="forfeit"` and `forfeit_by_index=player_index` on forfeit
- Tests: `test_ux003.py` (13 tests: model fields, compute_round_result normal reason, SD cap draw reason, forfeit API metadata); updated `test_scoring.py` equality assertions to include `completion_reason="normal"`; 457 total tests pass
- Files: `app/models/game.py`, `app/rules/reducer.py`, `app/services/game_service.py`, `tests/test_ux003.py`, `tests/test_scoring.py`, `ralph/prd.json`, `ralph/progress.txt`
- Pattern: adding optional fields with `None` default to Pydantic models is safe backward-compat; existing tests that do `== GameResult(...)` exact equality need updating when new fields are added
---

## 2026-03-01 14:10 - US-UX-002
- Added `email: str | None = None` to `PlayerState` model
- Added `get_caller_email` dependency to `app/auth.py` (returns email from JWT, never raises)
- Updated `game_service.create_game` and `join_game` to accept and store `email` param
- Updated `routers/games.py` create_game and join_game to inject `CallerEmailDep` and forward email
- Tests: `test_ux002.py` (7 tests: email stored on create/join, preserved through deck-deal, GET/list return emails, spectator blocking unchanged); 444 total tests pass
- Files: `app/auth.py`, `app/models/game.py`, `app/routers/games.py`, `app/services/game_service.py`, `tests/test_ux002.py`, `ralph/prd.json`, `ralph/progress.txt`
- Pattern: `get_caller_email` is separate from `get_caller_id` and returns `None` (not 401) when header is absent — safe to add to existing endpoints without breaking legacy test fixtures that only override `get_caller_id`
---

## 2026-03-01 13:46 - US-UX-001
- Added `class="dark"` to `<html>` in `index.html` for dark mode default
- Updated `Games.tsx` header to `flex-col sm:flex-row` for mobile-first stacking; added `cursor-pointer` to game list row buttons
- Updated `GameRoom.tsx`: responsive board cell sizes (`w-16 sm:w-20`), mobile-friendly invite link layout, dark mode variants for board colors, mists feedback, combo indicator, and win/loss labels
- Tests: added `ux001.test.tsx` (Red→Green on cursor-pointer); 78 unit tests + 437 backend tests all pass
- Files: `web/index.html`, `web/src/routes/Games.tsx`, `web/src/routes/GameRoom.tsx`, `web/src/__tests__/ux001.test.tsx`, `ralph/prd.json`, `ralph/progress.txt`
- Pattern: tailwind v4 + `darkMode: ["class"]` → `dark:` prefix works; add `dark` to `<html>` statically in `index.html`
---
