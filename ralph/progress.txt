# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-02-26
---

## 2026-02-26 21:50 - US-UI-001
- Scaffolded Vite 7 + React 19 + TypeScript app in `web/` with React Router 7, Tailwind CSS 4, and manual shadcn/ui Button component
- Configured Vite dev proxy (`/api/*` → `http://localhost:8000/*`), vitest 4 test suite (6 tests pass), and production build (tsc -b && vite build)
- Added web dev/build/test commands to AGENTS.md and web/README.md; fixed .gitignore to allow `web/src/lib/`
- Files changed: `web/` (all scaffold files), `AGENTS.md`, `.gitignore`
- Learnings: bun resolves latest versions (React 19, Router 7, Vite 7, Tailwind 4, Vitest 4) — use `vitest/config` defineConfig (not vite's) for proper test type resolution; Tailwind v4 uses `@import "tailwindcss"` + `@tailwindcss/postcss` plugin; `bun test` runs bun's runner, not vitest — use `bun run test` to invoke the vitest script
---

