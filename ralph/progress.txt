# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-02-25
---

## 2026-02-25 18:45 - US-001
- Created `pyproject.toml` (uv/hatchling, fastapi, uvicorn, pytest, httpx, ruff, pyright)
- Created `app/main.py` (FastAPI app with `/health` endpoint) and `tests/test_health.py`
- Updated `AGENTS.md` with canonical `uv run` commands for install, dev server, lint, typecheck, and tests
- Tests/validation run: `uv run pytest` → 1 passed; `uv run ruff check .` → clean
- Files changed: `pyproject.toml`, `uv.lock`, `app/__init__.py`, `app/main.py`, `tests/__init__.py`, `tests/test_health.py`, `AGENTS.md`
- Learnings: hatchling needs `[tool.hatch.build.targets.wheel] packages = ["app"]` when package name doesn't match project name; uv not pre-installed — installed via pip from mise Python
---

## 2026-02-25 19:30 - US-003
- Created `app/rules/captures.py`: pure `resolve_captures()` using a static adjacency table; `mists_modifier` param ready for US-004
- Created `app/rules/reducer.py`: `PlacementIntent` dataclass + `apply_intent()` (basic: places card, resolves captures, bumps state_version, no Mists/turn-validation yet)
- Tests/validation run: `uv run pytest` → 35 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/__init__.py`, `app/rules/captures.py`, `app/rules/reducer.py`, `tests/test_captures.py`
- Learnings: helper wrapper `rc()` in test file keeps call sites under 100 chars without losing clarity; `model_copy(update=...)` is the Pydantic v2 immutable-update pattern; board adjacency direction pairs: placed card uses direction side, neighbor uses opposite (N↔S, E↔W)
---

## 2026-02-25 20:00 - US-004
- Added `mists_modifier_from_roll(roll: int) -> int` (public) to `reducer.py`: 1→-1, 6→+1, 2-5→0
- Updated `apply_intent` to roll `rng.randint(1, 6)` when rng is provided; passes modifier to `resolve_captures`; rng=None → modifier=0 for backward compat
- Tests/validation run: `uv run pytest` → 48 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/reducer.py`, `tests/test_mists.py`
- Learnings: `captures.py` already had `mists_modifier` param from US-003 with forward comment; ruff I001 import-sort triggered by stdlib before third-party ordering; `MagicMock().randint.return_value` lets tests pin the roll without a real Random instance
---

## 2026-02-25 19:00 - US-002
- Created `app/models/cards.py` (CardSides, CardDefinition with pydantic validation)
- Created `app/models/game.py` (Archetype, GameStatus enums; BoardCell, PlayerState, GameResult, GameState models)
- Tests/validation run: `uv run pytest` → 19 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/models/__init__.py`, `app/models/cards.py`, `app/models/game.py`, `tests/test_models.py`
- Learnings: pydantic is available transitively via FastAPI; `[None] * 9` inferred as `list[None]` by pyright — must annotate as `list[BoardCell | None]` explicitly; `set` is valid as a pydantic field name (class annotation, no actual shadowing issue)
---
