# Ralph Progress Log

## Codebase Patterns
- balance errors in validate_card_balance return list[str]; loader wraps them in CardLoadError with line number
- Test fixtures that create cards with specific sides must now satisfy min(sides) ≤ 3

---
Started: 2026-03-01
---

## 2026-03-01 - US-GM-001
- Added weak-side validation to `validate_card_balance` in `app/rules/cards.py`: min(sides) must be ≤ 3
- Fixed 43 cards in `cards.jsonl` that violated the rule (reduced lowest side to 3, added deficit to highest non-capped side); budget sums preserved
- Updated 4 test fixtures in `test_cards_balance.py` that used min-side ≥ 4; documented weak-side rule in `CARDS_SPEC.md`
- Tests run: `uv run pytest` — 490 passed
- Files changed: `app/rules/cards.py`, `cards.jsonl`, `tests/test_gm001_weak_side.py`, `tests/test_cards_balance.py`, `CARDS_SPEC.md`
- Learnings: T3 ultra cards (budget=32, cap=10) all converge to n=10, e=10, s=9, w=3 or similar; that's unavoidable with the strict sum+cap constraints. Tests that test ONLY budget/cap failures need sides with min≤3 to avoid dual-failure interference.
---

## 2026-03-01 - US-GM-003
- Added Plus rule as pre-step in `resolve_captures`: computes raw side-sums for adjacent opponent cells; if 2+ share the same sum, captures all matching cells immediately and enqueues them for BFS combo
- Changed `resolve_captures` return type to `tuple[list[BoardCell | None], bool]`; updated all callers (reducer, test_captures rc wrapper, test_combo_captures direct calls)
- Added `plus_triggered: bool = False` to `LastMoveInfo`; reducer passes it from resolve_captures result
- Fixed pre-existing `test_presence.py` test that coincidentally produced equal Plus sums (changed placed.e from 5 to 4 to differentiate sums)
- Tests run: `uv run pytest` — 502 passed; Files: `app/rules/captures.py`, `app/rules/reducer.py`, `app/models/game.py`, `tests/test_gm003_plus_rule.py`, `tests/test_captures.py`, `tests/test_combo_captures.py`, `tests/test_presence.py`
- Learnings: Card sides are bounded 1-10 by CardSides validation — test cards exceeding 10 fail Pydantic validation. When adding pre-steps to BFS, watch for unintended Plus triggers in existing tests that use equal card values (sum matches coincidentally).
---

## 2026-03-02 - US-GM-004
- Added `plus_triggered?: boolean` to `LastMoveInfo` in `web/src/lib/api.ts`
- Added cyan "Plus!" callout in `ActiveGameView.tsx` rendered when `game.last_move?.plus_triggered === true`; callout is visually distinct from amber/purple capture feedback; has `aria-live="polite"` and dark mode classes
- Added 6 tests in `tests/gm004.test.tsx`: Plus! shown on true; hidden on false; hidden when absent; coexists with capture feedback; has aria-live; has dark: classes
- Tests run: `bun run test --run` — 157 passed
- Files changed: `web/src/lib/api.ts`, `web/src/routes/game-room/ActiveGameView.tsx`, `web/src/__tests__/gm004.test.tsx`
- Learnings: `plus_triggered` as optional (`?`) in the TS interface handles backward compat naturally; `=== true` check ensures undefined/absent field shows no callout
---

## 2026-03-02 - US-GM-005
- Added `element: ElementType` (Literal["blood","holy","arcane","shadow","nature"]) to `CardDefinition` in `app/models/cards.py`; validation is Pydantic-native (missing/invalid element = load error)
- Assigned element to all 65 cards in `cards.jsonl` using `ralph/card_elements.json` as sole source; created migration `supabase/migrations/004_add_element_to_cards.sql`; updated `card_to_row` in `scripts/seed_cards.py` and updated CARDS_SPEC.md
- Updated 36 test fixtures (all `_make_card`/`make_card`/`_card` helpers across existing test files) to include `element="shadow"`; wrote 18 new tests in `tests/test_gm005_elemental_data.py`
- Tests run: `uv run pytest` — 521 passed
- Files changed: `app/models/cards.py`, `cards.jsonl`, `scripts/seed_cards.py`, `supabase/migrations/004_add_element_to_cards.sql`, `tests/test_gm005_elemental_data.py`, `tests/test_seed_cards.py`, `CARDS_SPEC.md`, + 34 test fixture files
- Learnings: `element` as a required Pydantic `Literal` field is the cleanest validation approach — the model itself rejects bad values, no loader-level check needed beyond what Pydantic provides. All module-level CardDefinition constructions in tests will fail collection if element is missing — easier to catch than runtime-only failures.
---

## 2026-03-02 - US-GM-006
- Added `board_elements: list[str] | None = None` to `GameState`; added `elemental_triggered: bool = False` to `LastMoveInfo`
- Generated `board_elements` (9 cells) deterministically in `join_game` via `Random(seed).choices([...], k=9)` when second player joins; computed `elemental_bonus = 1` in reducer when cell element matches placed card element, stacked onto `mists_modifier` before `resolve_captures`; set `elemental_triggered` in `LastMoveInfo`
- 13 new tests in `tests/test_gm006_elemental.py`; 534 total passing
- Files changed: `app/models/game.py`, `app/services/game_service.py`, `app/rules/reducer.py`, `tests/test_gm006_elemental.py`
- Learnings: Elemental bonus stacks naturally into the existing `mists_modifier` parameter — no new param needed. Both `plus_triggered` and `elemental_triggered` follow the same optional-bool-with-default pattern in `LastMoveInfo` for backward compat. `Random(seed).choices(...)` and `Random(seed).randint(...)` in `join_game` are independent instances so no state sharing issues.
---

## 2026-03-02 - US-GM-007
- Added `board_elements: string[] | null` to `GameState` and `elemental_triggered?: boolean` to `LastMoveInfo` in `web/src/lib/api.ts`; added `element?: string` to `CardDefinition`
- Updated `BoardGrid.tsx`: element symbol badges (🩸 ✦ ✧ ◆ ✿) on every cell (aria-label="element {name}"); emerald ring highlight on cells matching selected card's element; passes `boardElements` and `selectedCardElement` as optional props
- Added Elemental! callout in `ActiveGameView.tsx` (gold/yellow, distinct from Plus! cyan and amber capture); shows element name ("Blood Elemental!"); updated `GameRoom.tsx` to compute `selectedCardElement` from selected card's def
- 12 new tests in `gm007.test.tsx`: direct BoardGrid tests for labels and highlight; GameRoom-level tests for callout; 169 total passing
- Files changed: `web/src/lib/api.ts`, `web/src/routes/game-room/BoardGrid.tsx`, `web/src/routes/game-room/ActiveGameView.tsx`, `web/src/routes/GameRoom.tsx`, `web/src/__tests__/gm007.test.tsx`
- Learnings: Direct component tests (rendering `<BoardGrid>` without full router) are cleaner for visual/structural tests; GameRoom-level tests for behavioral callouts. ring-emerald conflicts with ring-yellow are avoided with `!isLastMove` guard. IIFE in JSX works for inline computation but helper vars before return are cleaner.
---

## 2026-03-02 - US-GM-008
- Updated `CLAUDE.md` Key Domain Concepts: added weak-side rule description, Plus rule entry, Elemental system entry (5 elements, match=+1, Plus/combo use raw values)
- Rewrote `GAME_RULES_OVERVIEW.md`: fixed Mists from ±1 to ±2; fixed Devout to say "negate the −2"; added weak-side rule to Components/Cards; added full Plus Rule section with example; added full Elemental System section with thematic mapping; updated MVP Defaults to reflect all implemented systems; removed out-of-scope callouts for now-implemented features
- `CARDS_SPEC.md`: already up-to-date from US-GM-001 and US-GM-005 (no changes needed)
- `AGENTS.md`: verified commands unchanged (no changes needed)
- Tests run: `uv run pytest` — 534 passed; `bun run test --run` — 169 passed
- Files changed: `CLAUDE.md`, `GAME_RULES_OVERVIEW.md`
- Learnings: GAME_RULES_OVERVIEW.md had stale ±1 Mists values even after US-GM-002 updated the code — docs lag behind code changes unless explicitly refreshed. A dedicated docs story at sprint end is the right pattern.
---

## 2026-03-02 - US-GM-008 (second pass)
- Added Plus rule and Elemental System sections to `GameRulesDialog.tsx` (previously only docs/GAME_RULES_OVERVIEW.md had been updated; the in-game dialog was missing these two sections)
- Plus rule section explains sum-matching pre-capture with concrete example; Elemental section covers 5 elements, +1 bonus on cell match, and that Plus/combos use raw values
- Tests run: `uv run pytest` — 534 passed; `bun run test --run` — 169 passed
- Files changed: `web/src/routes/game-room/GameRulesDialog.tsx`, `ralph/prd.json`
- Learnings: When a PRD story is expanded in a later commit (e.g. adding a UI copy requirement to an otherwise-docs story), the passes flag is correctly reset — always re-read the current acceptance criteria rather than relying on progress log entries from a prior pass.
---

## 2026-03-02 - US-GM-009
- Exported `ELEMENT_SYMBOLS` from `BoardGrid.tsx`; imported it in `HandDrawer.tsx` and `CardInspectPreview.tsx` to keep element symbol mapping consistent across UI
- Added element badge (absolute bottom-left, aria-label="element {name}") to each hand card tile in `HandDrawer.tsx`; graceful: badge only renders when `def?.element` is present
- Added "Element: 🩸 Blood" row (or "Element: —" fallback) to `CardInspectPreview.tsx` between title and sides grid
- Added 3 new tests to `ux014.test.tsx`: preview shows element for card_001 (blood); preview shows "Element: —" for card without element; hand tile shows element badge aria-label; 172 frontend tests + 534 backend tests all pass
- Files changed: `web/src/routes/game-room/BoardGrid.tsx`, `web/src/routes/game-room/HandDrawer.tsx`, `web/src/routes/game-room/CardInspectPreview.tsx`, `web/src/__tests__/ux014.test.tsx`, `ralph/prd.json`
- Learnings: Exporting a `const` mapping from one component file so other components can reuse it avoids symbol drift; `aria-label="element {name}"` on element badges enables direct testing via `getByLabelText`.
---

## 2026-03-01 - US-GM-002
- Changed `mists_modifier_from_roll` in `app/rules/reducer.py`: Fog (roll 1) now returns -2, Omen (roll 6) now returns +2
- Updated `_mists_effect_label` to compare against ±2; updated Devout negation check to `mists_modifier == -2`
- Updated UI strings in `GameRulesDialog.tsx` and `ActiveGameView.tsx` from ±1 to ±2; updated `CLAUDE.md` Mists entry
- Tests run: `uv run pytest` — 490 passed; `bun run test --run` — 151 passed
- Files changed: `app/rules/reducer.py`, `tests/test_mists.py`, `tests/test_presence.py`, `web/src/routes/game-room/GameRulesDialog.tsx`, `web/src/routes/game-room/ActiveGameView.tsx`, `CLAUDE.md`
- Learnings: Integration test scenarios for fog/omen still work without adjusting card values since -2 still suppresses the same marginal captures and +2 still enables ties-to-capture. Only the direct modifier-value assertions needed changing.
---

