# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-03-05
---

## 2026-03-05 12:00 - US-SP-001
- Added `AIDifficulty` enum (easy/medium/hard/nightmare) and `player_type`/`ai_difficulty` fields to `PlayerState`
- Added `AI_PLAYER_ID` constant and `AI_DISPLAY_NAMES` dict to `game_service.py`
- 17 new tests covering enum values, model fields, backward compat, serialization roundtrip, and constants
- Tests/validation: `uv run pytest` 611 passed, `uv run pyright` 0 errors on changed files (5 pre-existing in test_scoring.py)
- Files changed: `app/models/game.py`, `app/services/game_service.py`, `tests/test_sp001_ai_model.py`
- Learnings: PlayerState defaults safely for old snapshots; Literal types validated by Pydantic
---

## 2026-03-05 13:00 - US-SP-002
- Added `create_game_vs_ai` service function with AI auto-draft (easy=random, medium=highest total, hard/nightmare=best coverage) and auto-archetype selection (easy=random, medium+=strategic Skulker/Martial)
- Added `_ai_auto_draft` and `_ai_auto_archetype` helper functions in `game_service.py`
- AI exempt from `_check_no_active_game` (uses `AI_PLAYER_ID` constant, different humans can play AI concurrently)
- Tests/validation: 19 new tests in `test_sp002_create_vs_ai.py`, `uv run pytest` 630 passed, `uv run pyright` 0 errors
- Files changed: `app/services/game_service.py`, `tests/test_sp002_create_vs_ai.py`
- Learnings: Used helper `_create()` in tests to keep lines under 100 chars; pre-existing E402 lint errors in game_service.py from imports after AI_DISPLAY_NAMES dict
---

## 2026-03-05 14:00 - US-SP-003
- Added `POST /games/vs-ai` endpoint with `CreateGameVsAiRequest` (difficulty field) in `app/routers/games.py`
- Added `createGameVsAi(difficulty)` function to `web/src/lib/api.ts` following existing error-handling pattern
- 10 new tests: 201 for all 4 difficulties, response status/fields validation, 409 when player has active game (AI or multiplayer)
- Tests/validation: `uv run pytest` 640 passed, `uv run pyright` 0 errors, `uv run ruff check` clean on changed files
- Files changed: `app/routers/games.py`, `tests/test_sp003_vs_ai_endpoint.py`, `web/src/lib/api.ts`
- Learnings: Endpoint must be registered before `/{game_id}` routes to avoid path param matching; test_ux002 pattern shows how to override `get_caller_email` in test fixtures
---

## 2026-03-05 15:00 - US-SP-004
- Created `app/rules/ai.py` with `choose_move()` stub (random legal move selection for now)
- Added `execute_ai_turn()` async function and `is_ai_turn()` helper to `game_service.py` with per-difficulty delay ranges
- Wired `BackgroundTasks` into `submit_draft` and `submit_move` endpoints to schedule AI turns
- 12 new tests: is_ai_turn (4 cases), choose_move (3 cases), execute_ai_turn no-ops (3 cases), AI move submission, full human→AI progression
- Tests/validation: `uv run pytest` 652 passed, `uv run pyright` 0 errors on changed files
- Files changed: `app/rules/ai.py`, `app/services/game_service.py`, `app/routers/games.py`, `tests/test_sp004_ai_turn_trigger.py`
- Learnings: MemoryGameStore uses `_states` dict (not `_games`); `asyncio.sleep` must be patched with `AsyncMock` for sync test runners; no pytest-asyncio in deps so used `asyncio.new_event_loop().run_until_complete()` wrapper
---

## 2026-03-05 16:00 - US-SP-005
- Implemented novice (Ireena/easy) AI strategy in `app/rules/ai.py`: score-based placement with capture awareness (+1 per capturable neighbor, +1 elemental match) plus uniform noise [0, 1.5], weighted random from top candidates
- Added archetype usage: 20% chance to skip entirely (deterministic from seed), 50% chance on 1st/2nd placement, random params per archetype type
- Refactored `choose_move` to dispatch by difficulty (novice for easy, random fallback for others)
- 21 new tests: valid moves, capture preference, noise suboptimality, elemental awareness, archetype usage patterns, all archetype types, dispatch
- Tests/validation: `uv run pytest` 673 passed, `uv run pyright` 0 errors, `uv run ruff check` clean
- Files changed: `app/rules/ai.py`, `tests/test_sp005_novice_ai.py`
- Learnings: `[] or default` is falsy in Python — use `x if x is not None else default` for list params; cell adjacency matters when asserting score comparisons (cell 6 adj to 3)
---

## 2026-03-05 17:00 - US-SP-006
- Implemented greedy (Rahadin/medium) AI strategy in `app/rules/ai.py`: one-ply simulation via `apply_intent`, picks move maximizing owned cells, breaks ties randomly
- Evaluates all archetype variants per move: Skulker (4 sides), Martial (CW/CCW), Intimidate (all valid adjacent targets), Caster/Devout (single variant)
- 7 new tests: capture preference, double vs single capture, Skulker usage, no-archetype-when-used, tiebreaking (deterministic + varied), Martial rotation
- Tests/validation: `uv run pytest` 680 passed, `uv run pyright` 0 errors, `uv run ruff check` clean
- Files changed: `app/rules/ai.py`, `tests/test_sp006_greedy_ai.py`, `tests/test_sp005_novice_ai.py` (renamed dispatch test)
- Learnings: Skulker +3 boost can exceed CardSides max (10) — test cards must have side values ≤7 for boosted sides; board setup for archetype tests needs constrained empty cells to force specific placements
---

## 2026-03-05 18:00 - US-SP-007
- Implemented expectimax (Strahd/hard) AI strategy: multi-ply search with opponent hand inference, sampling 25 opponent hands from the remaining card pool
- Search depth: full depth when ≤4 empty cells, capped at 4 otherwise; opponent nodes assume greedy play; heuristic eval uses cell ownership + side strength
- Evaluates archetype variants at every AI node (reuses `_greedy_archetype_variants`)
- Tests/validation: 8 new tests in `test_sp007_expectimax_ai.py`, `uv run pytest` 688 passed, `uv run pyright` 0 errors, `uv run ruff check` clean
- Files changed: `app/rules/ai.py`, `tests/test_sp007_expectimax_ai.py`
- Learnings: Mists randomness in simulation can negate skulker boosts — test card side values must account for fog (-2); multi-ply search may prefer different cells than greedy so tests need deterministic board setups (e.g. single empty cell)
---

## 2026-03-05 19:00 - US-SP-008
- Implemented MCTS (The Dark Powers/nightmare) strategy in `app/rules/mcts.py`: 2000 playouts, UCB1 selection, random rollouts to terminal, SimBoard lightweight simulation
- SimBoard bypasses Pydantic for speed: raw `(card_key, owner)` tuples per cell, BFS capture with cached side values
- Concealment layer: when visit counts within 10%, prefers mid-strength cards over strongest (sandbagging early game)
- Concurrency limiter: `asyncio.Semaphore(2)` gates concurrent nightmare computations; `run_in_executor` offloads compute
- Tests/validation: 12 new tests in `test_sp008_mcts_ai.py`, `uv run pytest` 700 passed, `uv run pyright` 0 errors, `uv run ruff check` clean
- Files changed: `app/rules/mcts.py`, `app/rules/ai.py`, `app/services/game_service.py`, `tests/test_sp008_mcts_ai.py`
- Learnings: SimBoard skips elemental bonus in rollouts for speed (minor accuracy tradeoff); MCTS completes 2000 iterations in <1s for typical board states; semaphore `_value` attr useful for testing
---

## 2026-03-05 20:00 - US-SP-009
- Added `ai_comment: str | None` field to `LastMoveInfo`; created `app/rules/ai_comments.py` with comment pools (10-20 lines each) for 4 characters × 6 triggers
- Wired commentary into `execute_ai_turn` (AI move triggers) and `submit_move` (human move reactions via `attach_human_move_reaction`); added `update_state` to `GameStore` protocol + both implementations
- Comment trigger conditions: ai_captured_cards, ai_got_captured, plus/elemental/archetype triggers, game_ending; each has 30-50% chance (randomized threshold + roll)
- Tests/validation: 49 new tests in `test_sp009_ai_comments.py`, `uv run pytest` 749 passed, `uv run pyright` 0 errors, `uv run ruff check` clean
- Files changed: `app/models/game.py`, `app/rules/ai_comments.py`, `app/services/game_service.py`, `app/store/__init__.py`, `app/store/memory.py`, `app/store/supabase_store.py`, `tests/test_sp009_ai_comments.py`
- Learnings: `update_state` needed as a lightweight snapshot update (no event append) for attaching metadata; Sequence[BoardCell|None] needed in test helpers to satisfy pyright list invariance
---

## 2026-03-05 21:15 - US-SP-010
- Reworked `Games.tsx` into three sections: Play vs AI (4 difficulty cards with character names + flavor text), My Games (shows AI character name + difficulty badge for AI games, email for human), Open Games (joinable lobbies with creator email + Join label + Create Game button)
- Regenerated `api-types.generated.ts` to include `player_type`, `ai_difficulty`, `AIDifficulty` fields; exported `AIDifficulty` type from `api.ts`
- Updated existing tests (ux001, gm011, routes, uxp014) to match new component structure; fixed mock data to include player lists
- Tests/validation: 7 new tests in `sp010.test.tsx`, `bun run test --run` 281 passed (39 files), `uv run pytest` 749 passed
- Files changed: `web/src/routes/Games.tsx`, `web/src/lib/api.ts`, `web/src/lib/api-types.generated.ts`, `web/src/__tests__/sp010.test.tsx`, `web/src/__tests__/ux001.test.tsx`, `web/src/__tests__/gm011.test.tsx`, `web/src/__tests__/routes.test.tsx`, `web/src/__tests__/uxp014.test.tsx`
- Learnings: When sections split games by participation, test data must include players matching the mock user ID; regenerated types change header format (openapi-typescript uses its own header)
---

## 2026-03-05 22:00 - US-SP-011
- Created shared `web/src/lib/ai-constants.ts` with `AI_DISPLAY_NAMES` and `AI_THINKING_TEXT` maps; updated `Games.tsx` to import from it
- Updated `ActionPanel.tsx` to show themed thinking text per AI difficulty ("Ireena is thinking...", "Rahadin calculates...", "Strahd contemplates...", "The Dark Powers stir...") instead of "Opponent's turn"
- Updated `ActiveGameView.tsx` score bar to show AI character name instead of "Opp"; updated `DraftingGameView.tsx` to show "Waiting for game to begin..." for AI games; updated `GameRoom.tsx` title to show AI character name
- Tests/validation: 10 new tests in `sp011.test.tsx`, `bun run test --run` 291 passed (40 files), `uv run pytest` 749 passed
- Files changed: `web/src/lib/ai-constants.ts`, `web/src/routes/Games.tsx`, `web/src/routes/GameRoom.tsx`, `web/src/routes/game-room/ActionPanel.tsx`, `web/src/routes/game-room/ActiveGameView.tsx`, `web/src/routes/game-room/DraftingGameView.tsx`, `web/src/__tests__/helpers.ts`, `web/src/__tests__/sp011.test.tsx`
- Learnings: `MuteToggle` component uses `isMuted` from sounds module — ActiveGameView tests must mock it; `makePlayer` helper extended with overrides param to support `player_type`/`ai_difficulty` fields
---

## 2026-03-05 23:00 - US-SP-012
- Created `AiCommentBubble` component with per-difficulty styling (amber/warm for Ireena, slate/cold for Rahadin, dark red for Strahd, violet with glow for Dark Powers)
- Bubble auto-fades after 3.5s, uses `pointer-events-none`, replaces immediately on new comment; integrated into `ActiveGameView` above the board
- 10 new tests: bubble renders/hides, fade timeout, comment replacement, per-difficulty styling (parameterized), pointer-events-none, ActiveGameView integration (with/without comment, human-vs-human)
- Tests/validation: `bun run test --run` 303 passed (41 files), `uv run pytest` 749 passed
- Files changed: `web/src/routes/game-room/AiCommentBubble.tsx`, `web/src/routes/game-room/ActiveGameView.tsx`, `web/src/__tests__/sp012.test.tsx`
- Learnings: `vi.useFakeTimers()` + `act(() => vi.advanceTimersByTime())` needed for testing auto-fade; AnimatePresence mock must pass children through for exit animations to be testable
---

## 2026-03-05 17:30 - US-SP-013
- Updated CLAUDE.md: added Single Player section to Key Domain Concepts with all 4 AI difficulty descriptions, AI turn trigger, and commentary system; updated layered structure to include ai.py, mcts.py, ai_comments.py
- Updated GAME_RULES_OVERVIEW.md: added Single Player Mode section with AI Opponents table, AI Turn Flow, and AI Commentary descriptions
- Archived ralph/plan-single-player.md to ralph/archive/2026-03-05-single-player/ (gitignored, local only); deleted from tracked files
- No stale "multiplayer only" references found in docs; api.ts has no JSDoc comments needing updates
- Tests/validation: `uv run pytest` 749 passed, `bun run test --run` 303 passed (41 files)
- Files changed: `CLAUDE.md`, `docs/GAME_RULES_OVERVIEW.md`, `ralph/plan-single-player.md` (deleted)
- Learnings: `ralph/archive/` is gitignored so archived files are local-only; documentation stories are straightforward but should still verify no stale references exist
---

## 2026-03-05 17:00 - US-SP-014
- Added archetype guard to `is_ai_turn()`: returns False if any player has `archetype is None`, preventing AI from moving before human selects archetype
- Moved AI turn trigger from `POST /{game_id}/draft` to `POST /{game_id}/archetype` endpoint (added `BackgroundTasks` + `CardStoreDep` dependencies)
- Removed `BackgroundTasks`/`CardStoreDep` from draft endpoint (no longer needed there)
- Updated `test_sp004` to set archetypes in test helpers; 5 new tests in `test_sp014_archetype_ai_guard.py`
- Tests/validation: `uv run pytest` 754 passed, `uv run pyright` 0 errors, `uv run ruff check` clean (pre-existing E402 only)
- Files changed: `app/services/game_service.py`, `app/routers/games.py`, `tests/test_sp004_ai_turn_trigger.py`, `tests/test_sp014_archetype_ai_guard.py`
- Learnings: `_make_ai_state` helpers in tests must include archetype fields now; draft endpoint no longer needs card_store or background_tasks deps
---

