# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-03-03
---

## 2026-03-03 — US-PR-001
- Changed Caster reroll from "discard first, use second" to `max(roll1, roll2)` (best-of-two)
- Updated tests: best-of-two picks higher roll, keeps first when higher, deterministic with seeded RNG
- Tests/validation: 568 passed, ruff clean on changed files, pyright pre-existing errors only
- Files changed: `app/rules/reducer.py`, `tests/test_caster.py`
- Learnings: mock_rng returns values in sequence; max() on raw die values is correct since higher=better (1=Fog worst, 6=Omen best)
---

## 2026-03-03 — US-PR-002
- Devout `activated` set to False at dispatch; only set True when Fog actually negated
- Added `archetype_activated = True` inside the `devout_negate_fog and mists_modifier == -2` block
- Tests: non-Fog → power NOT consumed, Fog → consumed, retry across multiple turns until Fog
- Tests/validation: 568 passed, ruff clean, pyright pre-existing only
- Files changed: `app/rules/reducer.py`, `tests/test_devout.py`
- Learnings: conditional consume pattern — set flag at dispatch, consume at effect site
---

## 2026-03-04 — US-PR-003
- Changed Skulker boost from +2 to +3 in `apply_skulker_boost`
- Updated docstring, test assertions, test names, and comments to reflect +3
- Updated docs (CLAUDE.md, GAME_RULES_OVERVIEW.md) and frontend copy (archetypesCopy.ts)
- Tests/validation: 568 passed, ruff clean on changed files, pyright pre-existing only
- Files changed: `app/rules/archetypes.py`, `tests/test_skulker.py`, `CLAUDE.md`, `GAME_RULES_OVERVIEW.md`, `web/src/routes/game-room/archetypesCopy.ts`
- Learnings: straightforward value change; remember to update UI copy strings too
---

## 2026-03-04 — US-PR-004
- Added `rotate_card_ccw` to archetypes.py (CCW: new.n=old.e, new.e=old.s, new.s=old.w, new.w=old.n)
- Added `martial_rotation_direction` field to PlacementIntent, MoveRequest, and game_service.submit_move
- Reducer validates direction ('cw'/'ccw'), defaults to 'cw' for backward compat
- Frontend: CW/CCW toggle in ActionPanel for Martial, wired through GameRoomContext → GameRoom → api.ts
- Updated canPlace guard in ActiveGameView to require direction for Martial
- Backend: 7 new tests (CCW unit, CW/CCW integration, default-to-CW, invalid direction error)
- Frontend: 2 new tests (CW/CCW toggle renders, submits martial_rotation_direction)
- Tests/validation: 575 backend passed, 247 frontend passed, ruff clean, pyright pre-existing only
- Files changed: archetypes.py, reducer.py, games.py, game_service.py, test_martial.py, api.ts, ActionPanel.tsx, ActiveGameView.tsx, GameRoomContext.tsx, GameRoom.tsx, archetypesCopy.ts, routes.test.tsx, CLAUDE.md, GAME_RULES_OVERVIEW.md
- Learnings: reused powerSide state for CW/CCW (strings "cw"/"ccw") to avoid adding new context fields
---

