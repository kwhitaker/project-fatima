# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-03-06
---

## 2026-03-06 14:00 - US-PE-001
- Added `skulker_boost_side` and `martial_rotation_direction` fields to `LastMoveInfo` model
- Populated fields from intent in `apply_intent()` (only when archetype activated)
- Regenerated `api-types.generated.ts` with new fields in both LastMoveInfo and MoveRequest schemas
- Tests: 21 archetype feedback tests pass (10 new), 857 total tests pass, pyright clean (pre-existing errors only)
- Files: `app/models/game.py`, `app/rules/reducer.py`, `tests/test_sp018_archetype_feedback.py`, `web/src/lib/api-types.generated.ts`
- `devout_ward_cell` already covered by existing `warded_cell` field on LastMoveInfo
---

## 2026-03-06 16:15 - US-PE-002
- Added `martialRotationDirection` prop to `BoardGrid`; placed card animates 90° CW/CCW rotation with spring overshoot + sweep ring effect
- Updated `BoardCallouts` to display "Martial Spin!" instead of generic "Martial!" for martial archetype
- Passed `martial_rotation_direction` from `ActiveGameView` → `BoardGrid`
- Added two DevPlayground scenarios (Martial CW at case 15, Martial CCW at case 16)
- Updated `sp018.test.tsx`: callout text expectations now match per-archetype labels; added `data-martial-spin` attribute tests for CW/CCW
- Tests: 346 frontend tests pass, 857 backend tests pass
- Files: `BoardGrid.tsx`, `BoardCallouts.tsx`, `ActiveGameView.tsx`, `DevPlayground.tsx`, `sp018.test.tsx`
- Pattern: use `data-martial-spin` data attribute for test assertions on animation state
---

## 2026-03-06 16:25 - US-PE-003
- Added `skulkerBoostSide` prop to BoardGrid; placed card shows emerald edge glow (`data-skulker-glow`) + '+3' particle (`data-skulker-particle`) on boosted side
- Updated BoardCallouts to display "Skulker +3!" instead of generic "Skulker!"
- Passed `skulker_boost_side` from ActiveGameView → BoardGrid
- Added 4 DevPlayground scenarios (Skulker N at case 14, E at 17, S at 18, W at 19); renumbered Devout Ward to 20 and Waiting to 21
- Tests: 351 frontend tests pass including new parameterized data-attribute tests for all 4 sides
- Files: `BoardGrid.tsx`, `BoardCallouts.tsx`, `ActiveGameView.tsx`, `DevPlayground.tsx`, `sp018.test.tsx`
- Pattern: use `data-skulker-glow` and `data-skulker-particle` data attributes for test assertions on skulker animations
---

## 2026-03-06 16:30 - US-PE-004
- Added `isCasterOmen` detection in BoardGrid; placed card shows purple radial pulse aura (`data-caster-aura`) + persistent shimmer + "+2 all sides" particle (`data-caster-particle`)
- Updated BoardCallouts to display "Caster Omen!" with violet/purple text + purple text-shadow instead of amber
- Added DevPlayground scenario (case 22) for Caster activation with omen mists
- Tests: 354 frontend tests pass, 857 backend tests pass
- Files: `BoardGrid.tsx`, `BoardCallouts.tsx`, `DevPlayground.tsx`, `sp018.test.tsx`
- Pattern: use `data-caster-aura` and `data-caster-particle` data attributes for test assertions on caster animations
---

## 2026-03-06 16:35 - US-PE-005
- Replaced emoji 🛡 ward indicator with animated golden shield overlay (`WardShieldOverlay` component in BoardGrid) with shield-up/steady/down animation states
- Changed ward cell ring color from sky to amber/gold (`ring-amber-300`); shield has radial gradient golden barrier with breathing pulse
- Updated BoardCallouts to show "Ward!" with golden/holy text-shadow instead of generic "Devout!"
- Updated DevPlayground scenario 20 description; updated pr006 and sp018 tests for new ring color and data-ward-shield attribute
- Tests: 357 frontend tests pass, 857 backend tests pass
- Files: `BoardGrid.tsx`, `BoardCallouts.tsx`, `DevPlayground.tsx`, `pr006.test.tsx`, `sp018.test.tsx`
- Pattern: use `data-ward-shield` attribute with value "up"/"steady"/"down" to track ward animation state in tests
---

## 2026-03-06 16:40 - US-PE-006
- Added `intimidateTargetCell` prop to BoardGrid; target cell shows shudder animation (`data-intimidate-shudder`), red flash overlay, and `-3` particle (`data-intimidate-particle`) on debuffed edge
- Updated BoardCallouts to display "Intimidate -3!" with red (`text-red-400`) + red text-shadow instead of generic amber
- Computed debuff side (facing side of target relative to placed card) in frontend from 3x3 grid positions
- Added DevPlayground scenario (case 23) for Intimidate with target shudder + debuff
- Passed `intimidateTargetCell` from ActiveGameView → BoardGrid via `game.last_move.intimidate_target_cell`
- Tests: 361 frontend tests pass, 857 backend tests pass
- Files: `BoardGrid.tsx`, `BoardCallouts.tsx`, `ActiveGameView.tsx`, `DevPlayground.tsx`, `sp018.test.tsx`
- Pattern: use `data-intimidate-shudder` and `data-intimidate-particle` data attributes for test assertions; compute debuff side from cell indices (target row/col vs placed row/col)
---

## 2026-03-06 16:42 - US-PE-007
- Added 5 archetype sound functions to `sounds.ts`: `playMartial()` (rising sweep), `playSkulker()` (staccato burst), `playCaster()` (deep hum), `playDevout()` (chime/bell), `playIntimidate()` (low rumble)
- Added useEffect in `ActiveGameView` to trigger appropriate sound when `archetype_used_name` detected in move update, using soundMap dispatch
- Updated sound mocks in 4 test files (sp018, pr006, sp012, sp011); updated uxp015 with export/pre-init/oscillator tests for all 5 new functions
- Tests: 362 frontend tests pass, 857 backend tests pass
- Files: `sounds.ts`, `ActiveGameView.tsx`, `uxp015.test.tsx`, `sp018.test.tsx`, `pr006.test.tsx`, `sp012.test.tsx`, `sp011.test.tsx`
- Pattern: use class-style mock for AudioContext in tests (vi.fn() wrapper doesn't work with `new`); sound mocks must include all exported functions to avoid import errors
---

