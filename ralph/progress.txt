# Ralph Progress Log

## Codebase Patterns
- (add stable patterns here as they are discovered)

---
Started: 2026-02-25
---

## 2026-02-25 18:45 - US-001
- Created `pyproject.toml` (uv/hatchling, fastapi, uvicorn, pytest, httpx, ruff, pyright)
- Created `app/main.py` (FastAPI app with `/health` endpoint) and `tests/test_health.py`
- Updated `AGENTS.md` with canonical `uv run` commands for install, dev server, lint, typecheck, and tests
- Tests/validation run: `uv run pytest` → 1 passed; `uv run ruff check .` → clean
- Files changed: `pyproject.toml`, `uv.lock`, `app/__init__.py`, `app/main.py`, `tests/__init__.py`, `tests/test_health.py`, `AGENTS.md`
- Learnings: hatchling needs `[tool.hatch.build.targets.wheel] packages = ["app"]` when package name doesn't match project name; uv not pre-installed — installed via pip from mise Python
---

## 2026-02-25 19:30 - US-003
- Created `app/rules/captures.py`: pure `resolve_captures()` using a static adjacency table; `mists_modifier` param ready for US-004
- Created `app/rules/reducer.py`: `PlacementIntent` dataclass + `apply_intent()` (basic: places card, resolves captures, bumps state_version, no Mists/turn-validation yet)
- Tests/validation run: `uv run pytest` → 35 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/__init__.py`, `app/rules/captures.py`, `app/rules/reducer.py`, `tests/test_captures.py`
- Learnings: helper wrapper `rc()` in test file keeps call sites under 100 chars without losing clarity; `model_copy(update=...)` is the Pydantic v2 immutable-update pattern; board adjacency direction pairs: placed card uses direction side, neighbor uses opposite (N↔S, E↔W)
---

## 2026-02-25 20:00 - US-004
- Added `mists_modifier_from_roll(roll: int) -> int` (public) to `reducer.py`: 1→-1, 6→+1, 2-5→0
- Updated `apply_intent` to roll `rng.randint(1, 6)` when rng is provided; passes modifier to `resolve_captures`; rng=None → modifier=0 for backward compat
- Tests/validation run: `uv run pytest` → 48 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/reducer.py`, `tests/test_mists.py`
- Learnings: `captures.py` already had `mists_modifier` param from US-003 with forward comment; ruff I001 import-sort triggered by stdlib before third-party ordering; `MagicMock().randint.return_value` lets tests pin the roll without a real Random instance
---

## 2026-02-25 19:00 - US-002
- Created `app/models/cards.py` (CardSides, CardDefinition with pydantic validation)
- Created `app/models/game.py` (Archetype, GameStatus enums; BoardCell, PlayerState, GameResult, GameState models)
- Tests/validation run: `uv run pytest` → 19 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/models/__init__.py`, `app/models/cards.py`, `app/models/game.py`, `tests/test_models.py`
- Learnings: pydantic is available transitively via FastAPI; `[None] * 9` inferred as `list[None]` by pyright — must annotate as `list[BoardCell | None]` explicitly; `set` is valid as a pydantic field name (class annotation, no actual shadowing issue)
---

## 2026-02-25 21:00 - US-006
- Created `app/rules/archetypes.py`: pure `rotate_card_once(card)` (new.n=old.w, new.e=old.n, new.s=old.e, new.w=old.s)
- Added `ArchetypeAlreadyUsedError` and `ArchetypeNotAvailableError` to `errors.py`
- Updated `PlacementIntent` with `use_archetype: bool = False`; reducer handles Martial validation + rotation before Mists/captures; sets `archetype_used=True` in player state
- Tests/validation run: `uv run pytest` → 73 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/archetypes.py` (new), `app/rules/errors.py`, `app/rules/reducer.py`, `tests/test_martial.py` (new)
- Learnings: `use_archetype` validation must fire before Mists roll; guard `if state.players` lets playerless mechanical tests pass unchanged; `dataclass field(default=False)` needed for default after positional args
---

## 2026-02-25 22:00 - US-007
- Added `apply_skulker_boost(card, side)` to `archetypes.py`: returns card copy with chosen side +2; original unchanged
- Added `ArchetypePowerArgumentError` to `errors.py` for missing/invalid archetype arguments
- Updated `PlacementIntent` with `skulker_boost_side: str | None = None`; refactored archetype block in `apply_intent` from MARTIAL-only check to dispatching if/elif/else covering MARTIAL and SKULKER
- Updated `test_martial.py`: changed SKULKER→CASTER in wrong-archetype test (SKULKER now has its own handler)
- Tests/validation run: `uv run pytest` → 88 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/archetypes.py`, `app/rules/errors.py`, `app/rules/reducer.py`, `tests/test_skulker.py` (new), `tests/test_martial.py`
- Learnings: moving `archetype_used` check before the archetype-type dispatch ensures all archetypes get `ArchetypeAlreadyUsedError` consistently; `model_dump()` + `CardSides(**dict)` is the clean pattern for selective field update; updating existing Martial test when a new archetype claims `use_archetype=True` is expected churn as handlers are added
---

## 2026-02-25 23:00 - US-008
- Added `elif player.archetype == Archetype.CASTER` branch in `apply_intent` archetype dispatch: sets `caster_reroll = True` and `archetype_activated = True`
- In Mists roll section: if `caster_reroll`, call `rng.randint` a second time; second result is used as the modifier input
- Updated `test_martial.py`: changed CASTER→DEVOUT in wrong-archetype test (CASTER now has its own handler)
- Tests/validation run: `uv run pytest` → 97 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/reducer.py`, `tests/test_caster.py` (new), `tests/test_martial.py`
- Learnings: the `caster_reroll` flag must be initialized before the archetype dispatch block so it is always in scope for the Mists roll section; `rng.randint.side_effect = [a, b]` allows pinning both rolls in sequence; updating the "wrong archetype" test is expected churn each time a new archetype handler is added — update to the next unimplemented archetype (DEVOUT for US-009)
---

## 2026-02-25 23:30 - US-009
- Added `devout_negate_fog` flag in `apply_intent`; DEVOUT branch sets it; post-roll logic zeros modifier when `devout_negate_fog and mists_modifier == -1`
- Updated `test_martial.py` wrong-archetype test: DEVOUT → PRESENCE (DEVOUT now has its own handler)
- Tests/validation run: `uv run pytest` → 108 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/reducer.py`, `tests/test_devout.py` (new), `tests/test_martial.py`
- Learnings: Devout applies after the Mists roll result is known, not before; the negation is a post-roll guard (`if devout_negate_fog and mists_modifier == -1`) — keeps the flag initialization pattern consistent with `caster_reroll`; updating the wrong-archetype test to the next unimplemented archetype (PRESENCE) is expected churn each time a new handler is added
---

## 2026-02-25 20:30 - US-005
- Created `app/rules/errors.py`: `InvalidMoveError` base + `WrongPlayerTurnError`, `CardNotInHandError`, `OccupiedCellError`
- Updated `app/rules/reducer.py`: added turn/hand/cell validation in `apply_intent`; removes card from hand and advances `current_player_index` after legal placement
- Tests/validation run: `uv run pytest` → 60 passed; `uv run ruff check .` → clean; `uv run pyright` → 0 errors
- Files changed: `app/rules/errors.py` (new), `app/rules/reducer.py`, `tests/test_turn_flow.py` (new)
- Learnings: guard turn/hand validation on `if state.players:` to keep existing pure-mechanics tests (which omit players) passing without modification; occupied-cell check is always enforced; `list.remove()` removes only the first occurrence, preserving duplicates in hand correctly
---
