# Ralph Progress Log

## Codebase Patterns
- balance errors in validate_card_balance return list[str]; loader wraps them in CardLoadError with line number
- Test fixtures that create cards with specific sides must now satisfy min(sides) ≤ 3
- shadcn/ui theme is defined entirely via CSS custom properties in web/src/index.css
- App uses dark mode by default (class="dark" on <html>)

---
Started: 2026-03-03
---

## 2026-03-03 12:55 - US-UXP-001
- Rewrote root README.md with project summary, prerequisites, backend/frontend setup, test/lint commands, seed data, and Further Reading links
- Trimmed AGENTS.md: removed duplicated setup/command sections (now in README), kept agent-specific workflow, code style, and useful pytest/ruff flags
- No existing .md files deleted
- Tests: 553 backend passed, 207 frontend passed
- Files changed: README.md, AGENTS.md
- Learnings: README already existed with basic content; rewrote rather than created fresh
---

## 2026-03-03 13:00 - US-UXP-002
- Replaced default shadcn/ui neutral palette with Castlevania NES-inspired colors in both :root and .dark blocks
- Dark mode: deep blue-black bg (240 20% 6%), warm parchment fg (40 20% 90%), blood-red primary (0 70% 45%), dark stone secondary (240 10% 15%), dull gold accent (45 60% 45%)
- Light mode: readable inverse with parchment bg, dark fg, same red primary
- Destructive uses orange-red (hue 25) to stay distinct from blood-red primary
- Tests: 207 frontend passed
- Files changed: web/src/index.css
- Learnings: CSS-variable-only change; all shadcn/ui components pick up theme automatically
---

## 2026-03-03 13:05 - US-UXP-003
- Added vampire emoji SVG favicon at web/public/favicon.svg
- Updated web/index.html: added favicon link, changed title from "Project Fatima" to "Yugi-Strahd"
- Searched frontend codebase for "Project Fatima" / "Fatima" in user-visible text — none found (only internal __FATIMA_ENV__ which is not user-facing)
- Tests: 207 frontend passed
- Files changed: web/public/favicon.svg (new), web/index.html
- Learnings: No user-visible "Project Fatima" references existed in .tsx files; rename was title-only
---

## 2026-03-03 13:10 - US-UXP-004
- Added Google Fonts (Press Start 2P + VT323) to index.html with preconnect
- Extended Tailwind config with font-heading and font-body families
- Set VT323 as body font (18px base) and Press Start 2P for h1-h6 in index.css
- Applied font-heading to: button component, ActionPanel turn label, ActiveGameView score/callouts, CompleteGameView results, HandPanel/HandDrawer labels
- Card stats, body text, dialog descriptions use VT323 (body default)
- Tests: 218 frontend passed, 553 backend passed
- Files changed: web/index.html, web/tailwind.config.js, web/src/index.css, web/src/components/ui/button.tsx, web/src/routes/game-room/ActionPanel.tsx, ActiveGameView.tsx, CompleteGameView.tsx, HandDrawer.tsx, HandPanel.tsx, web/src/__tests__/uxp004.test.tsx (new)
- Learnings: Press Start 2P is very small; 14px minimum for buttons works. VT323 at 18px base font-size provides good readability. Using CSS h1-h6 rule covers dialog titles automatically without touching component code.
---

## 2026-03-03 13:20 - US-UXP-005
- Set `--radius: 0px` in `:root` to eliminate all border-radius globally from shadcn/ui
- Replaced all hardcoded `rounded-*` Tailwind classes with `rounded-none` across 14 files (kept `rounded-full` only on the tiny realtime status dot)
- Added chunky borders: `border-[3px]` on board cells, `border-2` on hand cards, buttons, dialog frames, game list items, form inputs, drawer, archetype asides
- Button component updated: all variants get `border-2` with appropriate border-color per variant (ghost/link use `border-transparent`)
- Tests: 218 frontend passed
- Files changed: index.css, button.tsx, BoardGrid.tsx, HandPanel.tsx, HandDrawer.tsx, ArchetypeModal.tsx, ForfeitDialog.tsx, CardInspectPreview.tsx, GameRulesDialog.tsx, ArchetypePowerAside.tsx, Games.tsx, Login.tsx, ResetPassword.tsx, GameRoom.tsx
- Learnings: `--radius: 0px` handles shadcn components automatically but app code had 19 hardcoded `rounded-*` instances. The `border-2` addition on buttons required setting border-color per variant to avoid ghost/link variants showing unexpected borders.
---

## 2026-03-03 13:25 - US-UXP-006
- Changed card-placed animation timing from ease-out to steps(4) for 4-frame NES sprite pop
- Changed card-captured animation timing from ease-in-out to steps(3) for 3-frame brightness blink
- Added new @keyframes card-flip (scaleX: 1 → 0.1 → 1) with steps(3) at 0.3s for ownership flip effect
- Applied animate-card-flip alongside animate-card-captured on captured cells in BoardGrid.tsx
- Callout animations (Plus!, Elemental!) had no existing transitions — no changes needed
- Tests: 226 frontend passed
- Files changed: web/src/index.css, web/src/routes/game-room/BoardGrid.tsx, web/src/__tests__/uxp006.test.tsx (new)
- Learnings: steps(N) CSS timing divides animation into discrete frames with no interpolation — perfect for NES aesthetic. Callouts in ActiveGameView had no animations to convert.
---

## 2026-03-03 13:30 - US-UXP-007
- Added `.card-tier-2` (shiny gold/red conic-gradient border) and `.card-tier-3` (holographic prismatic border + color-dodge overlay) CSS classes
- Used `@property --tier-angle` with `:root` animation for gradient rotation — avoids animation conflicts with existing card-placed/card-captured animations on the same elements
- Tier 3 rotates at 2x speed via `calc(var(--tier-angle) * 2)` for faster-cycling prismatic effect
- Exported `tierClass()` helper from CardFace, applied in BoardGrid, HandPanel, HandDrawer, CardInspectPreview
- Refactored BoardGrid to compute `def` once at top of loop body (was duplicated in 3 branches)
- Tests: 240 frontend passed, 553 backend passed
- Files changed: web/src/index.css, CardFace.tsx, BoardGrid.tsx, HandPanel.tsx, HandDrawer.tsx, CardInspectPreview.tsx, web/src/__tests__/uxp007.test.tsx (new)
- Learnings: `@property` + `:root` animation is a clean pattern for rotating gradients that avoids per-element animation shorthand conflicts. `border-image` with `conic-gradient` works well with `rounded-none` (border-image doesn't support border-radius, but that's desired here).
---

## 2026-03-03 13:35 - US-UXP-008
- Installed `motion` (v12.34.5) as production dependency via `bun add motion`
- Replaced CSS `@keyframes card-placed` / `.animate-card-placed` with Motion spring animation (`motion.div` with `initial={{ scale: 0, opacity: 0 }}`, `animate={{ scale: 1, opacity: 1 }}`, spring stiffness 300, damping 20) in BoardGrid.tsx — only applied to cells in `placedCells` set
- Added `motion.button` with `whileHover={{ scale: 1.05 }}` on empty clickable board cells for interactive feedback
- Added stagger-animate-in for hand cards in both HandPanel.tsx and HandDrawer.tsx using `motion.div` variants with `staggerChildren: 0.05` and spring transitions (opacity 0→1, y 20→0)
- Updated uxp006 tests to remove card-placed CSS assertions (now handled by Motion)
- Tests: 248 frontend passed, 553 backend passed
- Files changed: web/package.json, web/bun.lock, web/src/index.css, BoardGrid.tsx, HandPanel.tsx, HandDrawer.tsx, uxp006.test.tsx (updated), uxp008.test.tsx (new)
- Learnings: `import { motion } from "motion/react"` is the correct import path for motion v12+. Spring animation with stiffness 300 / damping 20 gives a snappy pop with one subtle overshoot — good for card placement. Stagger parent needs `variants` with `staggerChildren` on the transition, children need `hidden`/`visible` variant keys.
---

## 2026-03-03 13:46 - US-UXP-013
- Added `character_key` to frontend `CardDefinition` type (backend already returns it)
- Bundled `ralph/card_emojis.json` as `web/src/lib/card-emojis.json` (single source of truth)
- Created `cardEmoji()` helper in CardFace.tsx that looks up emoji by character_key, returns undefined for missing keys
- Updated CardFace: emoji displayed prominently center (text-2xl/3xl), name shrunk below it; side values unchanged at edges
- Updated HandPanel, HandDrawer: emoji shown above card name with reduced name text size
- Updated CardInspectPreview: larger emoji (text-4xl) above card name in modal
- Graceful fallback: missing character_key → no emoji, layout unchanged
- Tests: 259 frontend passed, 553 backend passed
- Files changed: web/src/lib/api.ts, web/src/lib/card-emojis.json (new), CardFace.tsx, HandPanel.tsx, HandDrawer.tsx, CardInspectPreview.tsx, uxp013.test.tsx (new)
- Learnings: Backend already returns character_key in /api/cards — just needed to add it to the frontend type. JSON static import works cleanly with Vite+TS (resolveJsonModule already enabled). Multi-emoji strings (🌕🐺) render fine at text-2xl; no overflow handling needed at current card sizes.
---

## 2026-03-03 13:57 - US-UXP-010
- Replaced static `<button>` with `motion.button` for hand cards in HandPanel.tsx and HandDrawer.tsx
- Selected card animates to lifted state (scale 1.1, y: -8) with primary-color boxShadow glow; non-selected cards dim to opacity 0.7
- Added AnimatePresence for card exit animations (scale to 0 + fade) and layout prop for smooth gap-closing when cards leave the hand
- Added whileHover (scale 1.05, y: -4) for card preview lift; removed CSS transition-transform/hover:scale classes (now handled by motion)
- Updated ux009 test to reflect motion-based hover (no longer checks for CSS hover:scale class)
- Tests: 284 frontend passed, 553 backend passed
- Files changed: HandPanel.tsx, HandDrawer.tsx, uxp010.test.tsx (new), ux009.test.tsx (updated)
- Learnings: motion.div wrapper with `animate` prop handles selection state cleanly — conditional object for selected/hasSelection/default avoids class-based approach. `layout` prop on motion.div enables automatic position transitions when siblings are removed via AnimatePresence.
---

## 2026-03-03 13:53 - US-UXP-009
- Replaced CSS card-captured/card-flip animations with Motion-powered sequential capture chain in BoardGrid.tsx: captured cards flip (scaleX 1→0→1 with brightness flash) one by one with staggered delay (0.35s base + 0.3s per card)
- Placed card pulses (scale overshoot to 1.08) when captures exist, drawing attention to the source
- Added punch-in animations for Plus! and Elemental! callouts (scale 1.5→1 with spring) and capture feedback (scale 1.2→1) using AnimatePresence in ActiveGameView.tsx
- Score numbers animate with AnimatePresence popLayout (slide up/down on change)
- Updated uxp006/uxp008 tests to reflect CSS→Motion migration; added uxp009 test suite (13 assertions)
- Tests: 268 frontend passed, 553 backend passed
- Files changed: web/src/index.css, BoardGrid.tsx, ActiveGameView.tsx, uxp006.test.tsx, uxp008.test.tsx, uxp009.test.tsx (new)
- Learnings: Motion spring animations only support 2 keyframes — use `type: "tween"` with `times` array for multi-keyframe sequences (pulse, flip). `easeInOut` works well for the scaleX flip. AnimatePresence `mode="popLayout"` prevents layout jumps during score number swaps.
---

## 2026-03-03 14:02 - US-UXP-011
- Added slide-in entrance/exit animations (opacity 0→1, x 20→0) to last move callout and Mists feedback via motion.div + AnimatePresence in ActiveGameView.tsx
- Added turn indicator animation in ActionPanel.tsx: motion.p with AnimatePresence mode="wait" swaps between "Your turn" / "Opponent's turn" with scale pulse (0.9→1)
- Replaced instant mobile drawer show/hide with AnimatePresence + motion.div height animation (0→auto with spring)
- Existing callout animations (capture, Plus!, Elemental!, score) were already implemented in US-UXP-009 — verified still present
- Tests: 300 frontend passed, 553 backend passed
- Files changed: ActiveGameView.tsx, ActionPanel.tsx, uxp011.test.tsx (new)
- Learnings: Many sidebar animations were already done in US-UXP-009 (capture/Plus/Elemental callouts, score numbers). This story completed the remaining gaps: last move, Mists, turn indicator, and mobile drawer. AnimatePresence `initial={false}` on the drawer prevents animation on first mount.
---

## 2026-03-03 14:06 - US-UXP-012
- Added page entrance animations: Login and ResetPassword fade+slide up (motion.div, opacity 0→1, y 12→0, 0.2s); Games page fades in (0.15s)
- Games list items stagger in with motion.ul/motion.li variants (staggerChildren: 0.04, opacity 0→1, y 10→0)
- All 4 modals (ArchetypeModal, ForfeitDialog, CardInspectPreview, GameRulesDialog) now use AnimatePresence with scale+fade entrance/exit (scale 0.9→1, opacity 0→1, duration 0.15s). Backdrop fades independently
- Removed `if (!open) return null` early returns from ArchetypeModal, ForfeitDialog, GameRulesDialog — replaced with AnimatePresence conditional rendering
- CardInspectPreview wrapped in AnimatePresence at parent level (GameRoom.tsx) since it's conditionally rendered by parent
- Fixed ux014 test: added waitFor around close-button dialog removal assertion (AnimatePresence keeps element during exit animation)
- Tests: 310 frontend passed
- Files changed: Login.tsx, Games.tsx, ResetPassword.tsx, GameRoom.tsx, ArchetypeModal.tsx, ForfeitDialog.tsx, CardInspectPreview.tsx, GameRulesDialog.tsx, ux014.test.tsx, uxp012.test.tsx (new)
- Learnings: AnimatePresence with conditional `{open && (...)}` replaces `if (!open) return null` pattern cleanly. All durations kept at 0.15s for snappy feel. Existing tests that assert immediate DOM removal after close need `waitFor` when AnimatePresence is introduced.
---

## 2026-03-03 15:10 - US-UXP-016
- Replaced Presence archetype with Intimidate: after placing a card, choose one adjacent opponent card whose facing side is replaced with min(n,e,s,w) for this comparison only
- Backend: renamed PRESENCE→INTIMIDATE enum, replaced `presence_boost_direction` (str) with `intimidate_target_cell` (int 0-8) across PlacementIntent, MoveRequest, game_service, captures.py
- Reducer validates target cell is adjacent to placement, contains opponent card, and is not empty/own
- captures.py: substitutes neighbor's facing side with its min printed side when neighbor_index matches intimidate target
- Frontend: updated Archetype type, archetypesCopy, replaced N/E/S/W direction picker with two-step cell picker (click empty cell → click adjacent opponent card), added intimidatePendingCell state
- Tests: 556 backend passed, 310 frontend passed; 15 new test_intimidate.py tests covering debuff, validation, once-per-game
- Files changed: game.py, reducer.py, captures.py, games.py, game_service.py, test_intimidate.py (new), test_presence.py (deleted), test_combo_captures.py, api.ts, GameRoom.tsx, ActionPanel.tsx, ActiveGameView.tsx, HandDrawer.tsx, ArchetypeModal.tsx, GameRulesDialog.tsx, archetypesCopy.ts, routes.test.tsx, ux004.test.tsx, CLAUDE.md, GAME_RULES_OVERVIEW.md
- Learnings: Be careful with Plus rule in tests — equal sums across adjacent cells trigger Plus captures before standard comparisons, masking the intended test scenario. Use distinct sums in non-Plus tests.
---

## 2026-03-03 15:15 - US-UXP-017
- Updated button component: outline variant gets visible dark-mode border (muted-foreground/40), subtle bg tint (muted/30), hover:border-accent; ghost variant uses text-accent (gold) default color
- Promoted archetype selection buttons from outline → default (blood-red) variant
- Game list items hover: bg-muted → bg-accent/20 + border-accent for gold highlight
- Login/ResetPassword inputs: added hover:border-accent and focus:border-primary transitions
- Login mode toggle links: added text-accent (gold) color with hover:text-primary
- Replaced all hardcoded zinc-* color classes with theme tokens (bg-card, border-border, bg-muted-foreground/30) in 5 dialog/drawer components
- Info buttons (HandPanel, HandDrawer): added hover:border-accent
- Tests: 325 frontend passed, 556 backend passed; 15 new uxp017.test.tsx assertions
- Files changed: button.tsx, ArchetypeModal.tsx, Games.tsx, Login.tsx, ResetPassword.tsx, GameRulesDialog.tsx, ForfeitDialog.tsx, CardInspectPreview.tsx, HandDrawer.tsx, HandPanel.tsx, uxp017.test.tsx (new)
- Learnings: Many components used hardcoded zinc-* colors from shadcn/ui defaults instead of theme CSS custom properties. Converting to bg-card/border-border ensures all dialogs follow the Castlevania palette automatically.
---


## 2026-03-03 15:20 - US-UXP-018
- Added vampire emoji (🧛) as large visual centerpiece above login form (text-6xl, aria-hidden)
- Added "Yugi-Strahd" title with font-heading (Press Start 2P) and tagline "A Curse of Strahd Card Game" in font-body (VT323)
- Demoted form title (Sign in / Create account / Reset password) from h1 to h2, kept functional
- Tests: 330 frontend passed; 5 new uxp018 assertions
- Files changed: Login.tsx, uxp018.test.tsx (new)
- Learnings: Simple additive branding — no structural changes to form logic needed. font-heading/font-body utility classes from US-UXP-004 make font assignment trivial.
---

## 2026-03-03 15:25 - US-UXP-019
- Added slam overshoot: non-pulse placed cards use scale keyframes [0, 1.08, 1] for a decisive pop effect
- Added impact ripple: motion.div pseudo-element scales from 0.8→1.6 with opacity fade on placed cards
- Added board micro-shake: grid container is now motion.div with translateX oscillation [0, -2, 2, -1, 1, 0] keyed to placedCells
- Added Mists tint: CSS keyframes mists-fog-flash (blue inset glow) and mists-omen-flash (gold inset glow), applied via animate-mists-fog/animate-mists-omen classes on placed cards based on mistsEffect prop
- ActiveGameView passes game.last_move.mists_effect to BoardGrid when placement is active
- Updated uxp008 test to reflect slam overshoot change (no longer asserts spring params for non-pulse)
- Tests: 339 frontend passed; 11 new uxp019 assertions
- Files changed: BoardGrid.tsx, ActiveGameView.tsx, index.css, uxp008.test.tsx (updated), uxp019.test.tsx (new)
- Learnings: motion.div key trick for grid container allows re-triggering shake animation on each new placement. CSS box-shadow inset animation works well for brief color tints without affecting layout or z-ordering.
---

## 2026-03-03 15:30 - US-UXP-020
- Escalating brightness: each successive combo capture uses brightness(2 + captureSeq * 0.2) for increasingly dramatic flashes
- Ownership color wipe: semi-transparent blue/red overlay fades in/out during the scaleX flip midpoint, reinforcing color change
- Combo counter overlay: "+N" labels (font-heading, yellow, drop-shadow) appear on each captured cell during multi-captures
- Chain pulse ring: yellow border ring scales outward (0.5→1.3) and fades on each captured cell
- Custom events: `capture-step` CustomEvent dispatched at each capture delay with { cellIndex, step, total } detail for future sound system
- Tests: 345 frontend passed; 8 new uxp020 assertions
- Files changed: BoardGrid.tsx, uxp020.test.tsx (new)
- Learnings: Template literal in Motion animate prop works for dynamic brightness values. useRef tracks emitted event keys to avoid re-dispatching on re-renders. setTimeout aligns custom events with animation delays.
---

## 2026-03-03 15:35 - US-UXP-021
- Created VictoryOverlay component: fixed full-screen overlay with dark backdrop, VICTORY text in Press Start 2P with gold color + text-shadow, bat swarm (🦇 emoji particles), blood-red/gold confetti squares
- All animations use custom steppedEase() function (simulates CSS steps(N)) for pixel/NES aesthetic — bats use steps(8), confetti steps(6), VICTORY text steps(4)
- VICTORY text scale-in uses discrete keyframes [0, 0.5, 1.3, 1] for punch-in overshoot
- Added victory card glow: BoardGrid accepts `victoryCells` prop, applies `animate-victory-glow` CSS class with staggered animationDelay (0.2s per cell)
- CompleteGameView computes victoryCells (owned cells by winner) and passes to BoardGrid
- Auto-dismiss: 3s timeout or click-to-dismiss, with 400ms exit animation delay
- Tests: 360 frontend passed, 556 backend passed; 15 new uxp021 assertions
- Files changed: VictoryOverlay.tsx (new), CompleteGameView.tsx, BoardGrid.tsx, index.css, uxp021.test.tsx (new)
- Learnings: Motion doesn't support CSS steps() directly — custom easing function `(t) => Math.floor(t * steps) / steps` simulates it perfectly. Splitting glow into CSS animation (vs motion.div) avoids conflicts with existing motion props on board cells.
---

## 2026-03-03 16:05 - US-UXP-022
- Created DefeatOverlay.tsx: dark overlay (bg-black/80), slow DEFEAT fade-in (1.2s stepped), 18 drifting fog particles with blur, red vignette (radial-gradient), grayscale backdropFilter desaturation
- Integrated into CompleteGameView: detects isLoser, shows DefeatOverlay with auto-dismiss (3s) or click-dismiss
- Contrast with victory: defeat is slow/solemn (1.2s fade, no scale overshoot), fog drifts horizontally; victory is fast/punchy (0.5s scale pop, bats + confetti)
- Tests: 376 frontend passed, 556 backend passed; 16 new uxp022 assertions
- Files changed: DefeatOverlay.tsx (new), CompleteGameView.tsx, uxp022.test.tsx (new)
- Learnings: backdropFilter: grayscale() provides real-time desaturation behind overlay without affecting overlay content itself. Blur on fog particles (filter: blur(8px)) creates convincing mist at low particle counts.
---

## 2026-03-03 16:10 - US-UXP-023
- Added `early_finish` bool field to GameResult model (default False)
- Implemented early-finish check in reducer: after capture resolution, if `player_cells >= 5 + empty_cells`, game ends with early_finish=True
- Check runs before normal end-of-round check, works with captures creating clinch scenarios
- Frontend: GameResult type updated with `early_finish` field; CompleteGameView shows "Inevitable victory" / "The darkness claims this match" messages
- BoardGrid: empty cells on early-finish boards show ✕ marker with dashed border, dimmed opacity
- Tests: 562 backend passed (6 new), 385 frontend passed (9 new)
- Files changed: game.py, reducer.py, api.ts, BoardGrid.tsx, CompleteGameView.tsx, test_early_finish.py (new), uxp023.test.tsx (new)
- Learnings: The clinch formula `cells >= 5 + empty` is clean — on a 3x3 board you need 5+ to win, so if you own that many plus enough to cover all remaining empty cells, opponent can't catch up. Captures can push a player over the threshold in a single move.
---
